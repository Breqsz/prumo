# SEO Fase A — Fundação Técnica — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Consolidar a fundação técnica de SEO do site Prumo — grafo JSON-LD centralizado, sitemap enriquecido, correções semânticas, manifest/themeColor, OG de contato e config de domínio/GSC.

**Architecture:** Todo schema.org nasce de funções puras e tipadas em `lib/schema.ts` (single source of truth), consumidas pelas páginas via o componente existente `<JsonLd>`. Entidades linkadas por `@id` (`Organization` + `WebSite` site-wide no layout; `Service`, `CreativeWork`, `BreadcrumbList` por página). Config de domínio via env; nada de objeto JSON-LD inline duplicado.

**Tech Stack:** Next.js 16 App Router, TypeScript, Tailwind v4, Vitest + Testing Library, `next/og`. Sem novas dependências.

## Global Constraints

- Escopo: **nacional**, **técnico + conversão** (sem blog). Nenhuma página/schema de conteúdo editorial nesta fase.
- **Nunca inventar preço**: valores de `Offer` vêm parseados de `lib/plans.ts`; plano sem valor numérico → `Offer` sem `price`.
- **Não adicionar `keywords`** (Google ignora desde 2009).
- **Não inventar handle de Twitter/X** — só adicionar se Guilherme confirmar que existe.
- JSON-LD sempre via `<JsonLd data={...} />` de `@/components/seo/json-ld` (prop `data: Record<string, unknown>`).
- Sem framer-motion; site é CSS-only. Todas as páginas seguem SSG.
- `lang="pt-BR"` já é global; schemas usam `inLanguage: "pt-BR"`.
- Barra de qualidade ao fim de cada task: o comando de teste da task passa; ao fim do plano: `npm run lint` (0 erros), `npm run build` e `npx vitest run` verdes.
- Comandos rodam de `E:\projetos_breq\prumo\web`. Commits partem do root do repo `E:\projetos_breq\prumo`.

---

## File Structure

```
lib/schema.ts                       CREATE — funções puras de schema.org (single source of truth)
tests/lib/schema.test.ts            CREATE — testes de lib/schema.ts
app/layout.tsx                      MODIFY — render siteGraph() + viewport.themeColor + verification
app/page.tsx                        MODIFY — remover orgLd inline (migra p/ layout)
app/planos/page.tsx                 MODIFY — render servicesGraph()
app/trabalhos/[slug]/page.tsx       MODIFY — breadcrumbNode() + projectCreativeWorkNode()
app/trabalhos/page.tsx              MODIFY — + h1 + breadcrumbNode()
app/sobre/page.tsx                  MODIFY — migrar personLd inline p/ personNode()
app/sitemap.ts                      MODIFY — priority + changeFrequency
tests/app/sitemap.test.ts           CREATE — testes do sitemap
app/manifest.ts                     CREATE — web app manifest
tests/app/manifest.test.ts          CREATE — testes do manifest
app/contato/opengraph-image.tsx     CREATE — OG image própria de /contato
.env.example                        CREATE/MODIFY — documentar NEXT_PUBLIC_SITE_URL + GOOGLE_SITE_VERIFICATION
```

---

## Task 1: `lib/schema.ts` — builders de schema.org + testes

**Files:**
- Create: `lib/schema.ts`
- Test: `tests/lib/schema.test.ts`

**Interfaces:**
- Consumes: `SITE_URL` de `@/lib/site`; `CONTACT` de `@/lib/contact-config`; `CRIAR_PLANS`, `MANTER_PLANS`, `Plan` de `@/lib/plans`; `Project` de `@/lib/projects`.
- Produces:
  - `ORG_ID: string`, `WEBSITE_ID: string`
  - `organizationNode(): Record<string, unknown>`
  - `webSiteNode(): Record<string, unknown>`
  - `siteGraph(): Record<string, unknown>`
  - `personNode(): Record<string, unknown>`
  - `parsePriceBRL(price: string): number | null`
  - `servicesGraph(): Record<string, unknown>`
  - `projectCreativeWorkNode(project: Project): Record<string, unknown>`
  - `type Crumb = { name: string; url: string }`
  - `breadcrumbNode(items: Crumb[]): Record<string, unknown>`

- [ ] **Step 1: Write the failing test** — `tests/lib/schema.test.ts`

```ts
import { describe, it, expect } from "vitest";
import {
  ORG_ID,
  WEBSITE_ID,
  organizationNode,
  webSiteNode,
  siteGraph,
  personNode,
  parsePriceBRL,
  servicesGraph,
  projectCreativeWorkNode,
  breadcrumbNode,
} from "@/lib/schema";
import { CONTACT } from "@/lib/contact-config";
import { projects } from "@/lib/projects";

describe("organizationNode", () => {
  const org = organizationNode();
  it("is a ProfessionalService with a stable @id", () => {
    expect(org["@type"]).toBe("ProfessionalService");
    expect(org["@id"]).toBe(ORG_ID);
  });
  it("carries logo, email, areaServed and sameAs", () => {
    expect(String(org.logo)).toMatch(/\/prumo-logo\.png$/);
    expect(org.email).toBe(CONTACT.email);
    expect(org.areaServed).toBe("BR");
    expect(org.sameAs).toContain(CONTACT.linkedin);
  });
});

describe("webSiteNode", () => {
  it("links publisher to the organization @id", () => {
    const site = webSiteNode();
    expect(site["@type"]).toBe("WebSite");
    expect(site["@id"]).toBe(WEBSITE_ID);
    expect(site.publisher).toEqual({ "@id": ORG_ID });
    expect(site.inLanguage).toBe("pt-BR");
  });
});

describe("siteGraph", () => {
  it("wraps organization + website in an @graph", () => {
    const g = siteGraph();
    expect(g["@context"]).toBe("https://schema.org");
    expect(Array.isArray(g["@graph"])).toBe(true);
    expect((g["@graph"] as unknown[]).length).toBe(2);
  });
});

describe("personNode", () => {
  it("describes the founder and links worksFor to the org", () => {
    const p = personNode();
    expect(p["@type"]).toBe("Person");
    expect(p.name).toMatch(/Guilherme/);
    expect(p.worksFor).toEqual({ "@id": ORG_ID });
  });
});

describe("parsePriceBRL", () => {
  it("parses BRL strings to integers", () => {
    expect(parsePriceBRL("R$ 3.750")).toBe(3750);
    expect(parsePriceBRL("a partir de R$ 18.000")).toBe(18000);
    expect(parsePriceBRL("R$ 350")).toBe(350);
    expect(parsePriceBRL("R$ 1.350")).toBe(1350);
  });
  it("returns null when there is no number", () => {
    expect(parsePriceBRL("Sob consulta")).toBeNull();
  });
});

describe("servicesGraph", () => {
  const g = servicesGraph();
  const services = g["@graph"] as Array<Record<string, unknown>>;
  it("emits one Service per plan (3 criar + 3 manter)", () => {
    expect(services.length).toBe(6);
    for (const s of services) {
      expect(s["@type"]).toBe("Service");
      expect(s.provider).toEqual({ "@id": ORG_ID });
      expect(s.areaServed).toBe("BR");
    }
  });
  it("uses price for one-time plans and priceSpecification for monthly", () => {
    const landing = services.find((s) => String(s.name).startsWith("Landing"));
    const offerL = landing?.offers as Record<string, unknown>;
    expect(offerL.price).toBe(3750);
    expect(offerL.priceCurrency).toBe("BRL");

    const manut = services.find((s) => String(s.name).startsWith("Manutenção"));
    const offerM = manut?.offers as Record<string, unknown>;
    const spec = offerM.priceSpecification as Record<string, unknown>;
    expect(spec.unitText).toBe("MONTH");
    expect(spec.price).toBe(350);
    expect(offerM.price).toBeUndefined();
  });
});

describe("projectCreativeWorkNode", () => {
  it("maps a project to a CreativeWork linked to the org", () => {
    const node = projectCreativeWorkNode(projects[0]);
    expect(node["@type"]).toBe("CreativeWork");
    expect(node.name).toBe(projects[0].title);
    expect(node.creator).toEqual({ "@id": ORG_ID });
    expect(node.datePublished).toBe(String(projects[0].year));
  });
});

describe("breadcrumbNode", () => {
  it("numbers items from 1 with name + item", () => {
    const node = breadcrumbNode([
      { name: "Início", url: "https://x/" },
      { name: "Trabalhos", url: "https://x/trabalhos" },
    ]);
    const list = node.itemListElement as Array<Record<string, unknown>>;
    expect(list[0].position).toBe(1);
    expect(list[1].position).toBe(2);
    expect(list[1].name).toBe("Trabalhos");
    expect(list[1].item).toBe("https://x/trabalhos");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/lib/schema.test.ts`
Expected: FAIL — `Cannot find module '@/lib/schema'`.

- [ ] **Step 3: Write the implementation** — `lib/schema.ts`

```ts
import { SITE_URL } from "@/lib/site";
import { CONTACT } from "@/lib/contact-config";
import { CRIAR_PLANS, MANTER_PLANS, type Plan } from "@/lib/plans";
import type { Project } from "@/lib/projects";

export const ORG_ID = `${SITE_URL}/#organization`;
export const WEBSITE_ID = `${SITE_URL}/#website`;

const sameAs = [CONTACT.linkedin, CONTACT.instagram].filter(
  (v): v is string => Boolean(v),
);

export function organizationNode(): Record<string, unknown> {
  return {
    "@type": "ProfessionalService",
    "@id": ORG_ID,
    name: "Prumo",
    description:
      "Estúdio digital. Sites sob medida, estratégia e presença digital.",
    url: SITE_URL,
    logo: `${SITE_URL}/prumo-logo.png`,
    image: `${SITE_URL}/prumo-logo.png`,
    email: CONTACT.email,
    areaServed: "BR",
    sameAs,
  };
}

export function webSiteNode(): Record<string, unknown> {
  return {
    "@type": "WebSite",
    "@id": WEBSITE_ID,
    url: SITE_URL,
    name: "Prumo",
    inLanguage: "pt-BR",
    publisher: { "@id": ORG_ID },
  };
}

export function siteGraph(): Record<string, unknown> {
  return {
    "@context": "https://schema.org",
    "@graph": [organizationNode(), webSiteNode()],
  };
}

export function personNode(): Record<string, unknown> {
  return {
    "@context": "https://schema.org",
    "@type": "Person",
    name: "Guilherme Rocha Bianchini",
    jobTitle: "Fundador · Prumo",
    url: `${SITE_URL}/sobre`,
    email: CONTACT.email,
    worksFor: { "@id": ORG_ID },
    sameAs: [CONTACT.linkedin],
  };
}

// Preços em lib/plans.ts são strings tipo "R$ 3.750" / "a partir de R$ 18.000".
// Separador de milhar é ".", sem centavos — remover não-dígitos dá o inteiro certo.
export function parsePriceBRL(price: string): number | null {
  const digits = price.replace(/\D/g, "");
  return digits ? Number(digits) : null;
}

function serviceNode(plan: Plan): Record<string, unknown> {
  const amount = parsePriceBRL(plan.price);
  const recurring = /m[êe]s/i.test(plan.cadence);
  const offers: Record<string, unknown> = {
    "@type": "Offer",
    priceCurrency: "BRL",
  };
  if (amount !== null) {
    if (recurring) {
      offers.priceSpecification = {
        "@type": "UnitPriceSpecification",
        price: amount,
        priceCurrency: "BRL",
        unitText: "MONTH",
      };
    } else {
      offers.price = amount;
    }
  }
  return {
    "@type": "Service",
    name: `${plan.name} — Prumo`,
    description: plan.description,
    provider: { "@id": ORG_ID },
    areaServed: "BR",
    offers,
  };
}

export function servicesGraph(): Record<string, unknown> {
  return {
    "@context": "https://schema.org",
    "@graph": [...CRIAR_PLANS, ...MANTER_PLANS].map(serviceNode),
  };
}

export function projectCreativeWorkNode(
  project: Project,
): Record<string, unknown> {
  return {
    "@context": "https://schema.org",
    "@type": "CreativeWork",
    name: project.title,
    headline: project.title,
    abstract: project.summary,
    about: project.scope,
    url: `${SITE_URL}/trabalhos/${project.slug}`,
    datePublished: String(project.year),
    inLanguage: "pt-BR",
    creator: { "@id": ORG_ID },
  };
}

export type Crumb = { name: string; url: string };

export function breadcrumbNode(items: Crumb[]): Record<string, unknown> {
  return {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    itemListElement: items.map((item, i) => ({
      "@type": "ListItem",
      position: i + 1,
      name: item.name,
      item: item.url,
    })),
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/lib/schema.test.ts`
Expected: PASS (all describe blocks green).

- [ ] **Step 5: Commit**

```bash
cd /e/projetos_breq/prumo && git add web/lib/schema.ts web/tests/lib/schema.test.ts && git commit -m "feat(seo): lib/schema.ts com builders schema.org tipados + testes"
```

---

## Task 2: Site-wide graph no layout + themeColor + verification

**Files:**
- Modify: `app/layout.tsx`
- Modify: `app/page.tsx` (remover orgLd inline)

**Interfaces:**
- Consumes: `siteGraph()` de `@/lib/schema`; `JsonLd` de `@/components/seo/json-ld`.

- [ ] **Step 1: Editar `app/layout.tsx`** — adicionar imports, `viewport`, `verification` e render do grafo.

No topo, junto aos imports existentes, adicione:

```ts
import type { Metadata, Viewport } from "next";
import { JsonLd } from "@/components/seo/json-ld";
import { siteGraph } from "@/lib/schema";
```

(A linha `import type { Metadata } from "next";` existente vira a de `Metadata, Viewport` — não duplicar.)

No objeto `metadata`, adicione a chave `verification` logo após `twitter`:

```ts
  twitter: { card: "summary_large_image" },
  ...(process.env.GOOGLE_SITE_VERIFICATION
    ? { verification: { google: process.env.GOOGLE_SITE_VERIFICATION } }
    : {}),
```

Após o bloco `export const metadata`, adicione:

```ts
export const viewport: Viewport = {
  themeColor: "#000000",
};
```

Dentro de `<body ...>`, torne o `<JsonLd>` o primeiro filho:

```tsx
      <body className="min-h-screen bg-black text-white antialiased font-body">
        <JsonLd data={siteGraph()} />
        {children}
```

- [ ] **Step 2: Editar `app/page.tsx`** — remover o orgLd inline (agora vive no layout).

Remova as linhas de import não mais usadas:

```ts
import { JsonLd } from "@/components/seo/json-ld";
import { SITE_URL } from "@/lib/site";
import { CONTACT } from "@/lib/contact-config";
```

Remova o bloco `const sameAs = ...` e `const orgLd = { ... };` inteiro (linhas 28-40).

No JSX, remova a linha `<JsonLd data={orgLd} />`. O `return` passa a começar direto com `<Hero ... />`:

```tsx
export default function HomePage() {
  return (
    <>
      <Hero videoSrcs={HERO_VIDEOS} />
```

- [ ] **Step 3: Verificar typecheck + build**

Run: `npm run build`
Expected: build success. Sem erros de "unused import" (confirma que os imports removidos de page.tsx estavam certos).

- [ ] **Step 4: Verificar JSON-LD no HTML (dev)**

Run: `npm run dev` e em outro terminal `curl -s http://localhost:3000 | grep -o '"@type":"ProfessionalService"'`
Expected: retorna `"@type":"ProfessionalService"` (grafo site-wide presente). Encerrar o dev depois.

- [ ] **Step 5: Commit**

```bash
cd /e/projetos_breq/prumo && git add web/app/layout.tsx web/app/page.tsx && git commit -m "feat(seo): grafo Organization+WebSite site-wide no layout, themeColor e verification"
```

---

## Task 3: `Service`/`Offer` em `/planos`

**Files:**
- Modify: `app/planos/page.tsx`

**Interfaces:**
- Consumes: `servicesGraph()` de `@/lib/schema`; `JsonLd` de `@/components/seo/json-ld`.

- [ ] **Step 1: Editar `app/planos/page.tsx`** — adicionar imports e render.

Adicione aos imports:

```ts
import { JsonLd } from "@/components/seo/json-ld";
import { servicesGraph } from "@/lib/schema";
```

No JSX, torne o `<JsonLd>` o primeiro filho do fragment:

```tsx
  return (
    <>
      <JsonLd data={servicesGraph()} />
      <AmbientVideo srcs={AMBIENT_VIDEOS} spotlight>
```

- [ ] **Step 2: Build**

Run: `npm run build`
Expected: success.

- [ ] **Step 3: Verificar Services no HTML (dev)**

Run: `npm run dev` e `curl -s http://localhost:3000/planos | grep -o '"@type":"Service"' | wc -l`
Expected: `6`. Encerrar o dev.

- [ ] **Step 4: Commit**

```bash
cd /e/projetos_breq/prumo && git add web/app/planos/page.tsx && git commit -m "feat(seo): Service/Offer schema em /planos a partir de lib/plans"
```

---

## Task 4: `CreativeWork` + breadcrumb nos cases

**Files:**
- Modify: `app/trabalhos/[slug]/page.tsx`

**Interfaces:**
- Consumes: `breadcrumbNode`, `projectCreativeWorkNode` de `@/lib/schema`. Reusa `JsonLd` e `SITE_URL` já importados no arquivo.

- [ ] **Step 1: Editar `app/trabalhos/[slug]/page.tsx`**

Adicione ao bloco de imports:

```ts
import { breadcrumbNode, projectCreativeWorkNode } from "@/lib/schema";
```

Remova o objeto inline `const breadcrumbLd = { ... };` (o bloco `@type: "BreadcrumbList"` atual). Substitua a construção do breadcrumb por:

```tsx
  const breadcrumb = breadcrumbNode([
    { name: "Trabalhos", url: `${SITE_URL}/trabalhos` },
    { name: project.title, url: `${SITE_URL}/trabalhos/${slug}` },
  ]);
```

No JSX, troque `<JsonLd data={breadcrumbLd} />` por:

```tsx
      <JsonLd data={breadcrumb} />
      <JsonLd data={projectCreativeWorkNode(project)} />
```

- [ ] **Step 2: Build**

Run: `npm run build`
Expected: success (todas as rotas `/trabalhos/[slug]` prerenderizam).

- [ ] **Step 3: Verificar no HTML (dev)**

Run: `npm run dev` e `curl -s http://localhost:3000/trabalhos/hold-corretora | grep -o '"@type":"CreativeWork"'`
Expected: retorna `"@type":"CreativeWork"`. Encerrar o dev.

- [ ] **Step 4: Commit**

```bash
cd /e/projetos_breq/prumo && git add "web/app/trabalhos/[slug]/page.tsx" && git commit -m "feat(seo): CreativeWork + breadcrumb via lib/schema nos cases"
```

---

## Task 5: `h1` + breadcrumb em `/trabalhos`

**Files:**
- Modify: `app/trabalhos/page.tsx`

**Interfaces:**
- Consumes: `JsonLd` de `@/components/seo/json-ld`; `breadcrumbNode` de `@/lib/schema`; `SITE_URL` de `@/lib/site`.

- [ ] **Step 1: Editar `app/trabalhos/page.tsx`**

Adicione aos imports:

```ts
import { JsonLd } from "@/components/seo/json-ld";
import { breadcrumbNode } from "@/lib/schema";
import { SITE_URL } from "@/lib/site";
```

Substitua o `return` por:

```tsx
  return (
    <div className="flex h-screen flex-col overflow-hidden">
      <JsonLd
        data={breadcrumbNode([
          { name: "Início", url: `${SITE_URL}/` },
          { name: "Trabalhos", url: `${SITE_URL}/trabalhos` },
        ])}
      />
      <HeroNav />
      <h1 className="sr-only">Trabalhos selecionados — Prumo</h1>
      <main className="flex-1 overflow-hidden">
        <ProjectReel projects={projects} />
      </main>
    </div>
  );
```

- [ ] **Step 2: Build**

Run: `npm run build`
Expected: success.

- [ ] **Step 3: Verificar h1 único (dev)**

Run: `npm run dev` e `curl -s http://localhost:3000/trabalhos | grep -o '<h1' | wc -l`
Expected: `1`. Encerrar o dev.

- [ ] **Step 4: Commit**

```bash
cd /e/projetos_breq/prumo && git add web/app/trabalhos/page.tsx && git commit -m "fix(seo): h1 em /trabalhos + breadcrumb da listagem"
```

---

## Task 6: Migrar `Person` de `/sobre` para `lib/schema`

**Files:**
- Modify: `app/sobre/page.tsx`

**Interfaces:**
- Consumes: `personNode()` de `@/lib/schema`. Mantém `JsonLd`.

- [ ] **Step 1: Editar `app/sobre/page.tsx`**

Adicione ao import:

```ts
import { personNode } from "@/lib/schema";
```

Remova os imports que só serviam ao personLd inline:

```ts
import { SITE_URL } from "@/lib/site";
import { CONTACT } from "@/lib/contact-config";
```

Remova o objeto `const personLd = { ... };` (linhas 24-32).

No JSX, troque `<JsonLd data={personLd} />` por:

```tsx
      <JsonLd data={personNode()} />
```

- [ ] **Step 2: Build**

Run: `npm run build`
Expected: success, sem "unused import" (confirma remoção de SITE_URL/CONTACT).

- [ ] **Step 3: Commit**

```bash
cd /e/projetos_breq/prumo && git add web/app/sobre/page.tsx && git commit -m "refactor(seo): Person de /sobre via lib/schema (worksFor -> org)"
```

---

## Task 7: Sitemap enriquecido + testes

**Files:**
- Modify: `app/sitemap.ts`
- Test: `tests/app/sitemap.test.ts`

**Interfaces:**
- Produces: `default sitemap(): MetadataRoute.Sitemap` com `priority` + `changeFrequency` em toda entrada.

- [ ] **Step 1: Write the failing test** — `tests/app/sitemap.test.ts`

```ts
import { describe, it, expect } from "vitest";
import sitemap from "@/app/sitemap";
import { projects } from "@/lib/projects";
import { SITE_URL } from "@/lib/site";

describe("sitemap", () => {
  const entries = sitemap();

  it("includes every static route", () => {
    const urls = entries.map((e) => e.url);
    for (const path of ["/", "/planos", "/trabalhos", "/contato", "/sobre"]) {
      expect(urls).toContain(`${SITE_URL}${path}`);
    }
  });

  it("includes every project detail route", () => {
    for (const p of projects) {
      expect(
        entries.some((e) => e.url === `${SITE_URL}/trabalhos/${p.slug}`),
      ).toBe(true);
    }
  });

  it("sets priority and changeFrequency on every entry", () => {
    for (const e of entries) {
      expect(typeof e.priority).toBe("number");
      expect(e.changeFrequency).toBeDefined();
    }
  });

  it("gives the home page the highest priority", () => {
    const home = entries.find((e) => e.url === `${SITE_URL}/`);
    expect(home?.priority).toBe(1);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/app/sitemap.test.ts`
Expected: FAIL — `priority`/`changeFrequency` são `undefined` no sitemap atual.

- [ ] **Step 3: Reescrever `app/sitemap.ts`**

```ts
import type { MetadataRoute } from "next";
import { SITE_URL } from "@/lib/site";
import { projects } from "@/lib/projects";

type Entry = MetadataRoute.Sitemap[number];

export default function sitemap(): MetadataRoute.Sitemap {
  const now = new Date();

  const staticRoutes: Entry[] = [
    { url: `${SITE_URL}/`, lastModified: now, changeFrequency: "monthly", priority: 1 },
    { url: `${SITE_URL}/planos`, lastModified: now, changeFrequency: "monthly", priority: 0.9 },
    { url: `${SITE_URL}/trabalhos`, lastModified: now, changeFrequency: "monthly", priority: 0.8 },
    { url: `${SITE_URL}/contato`, lastModified: now, changeFrequency: "yearly", priority: 0.7 },
    { url: `${SITE_URL}/sobre`, lastModified: now, changeFrequency: "yearly", priority: 0.6 },
  ];

  const projectRoutes: Entry[] = projects.map((p) => ({
    url: `${SITE_URL}/trabalhos/${p.slug}`,
    lastModified: now,
    changeFrequency: "yearly",
    priority: 0.7,
  }));

  return [...staticRoutes, ...projectRoutes];
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/app/sitemap.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
cd /e/projetos_breq/prumo && git add web/app/sitemap.ts web/tests/app/sitemap.test.ts && git commit -m "feat(seo): sitemap com priority + changeFrequency + testes"
```

---

## Task 8: `app/manifest.ts` + testes

**Files:**
- Create: `app/manifest.ts`
- Test: `tests/app/manifest.test.ts`

**Interfaces:**
- Produces: `default manifest(): MetadataRoute.Manifest`.

- [ ] **Step 1: Write the failing test** — `tests/app/manifest.test.ts`

```ts
import { describe, it, expect } from "vitest";
import manifest from "@/app/manifest";

describe("manifest", () => {
  const m = manifest();

  it("has name, short_name and dark theme color", () => {
    expect(m.name).toMatch(/prumo/i);
    expect(m.short_name).toBe("Prumo");
    expect(m.theme_color).toBe("#000000");
    expect(m.background_color).toBe("#000000");
  });

  it("references at least one icon", () => {
    expect((m.icons ?? []).length).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/app/manifest.test.ts`
Expected: FAIL — `Cannot find module '@/app/manifest'`.

- [ ] **Step 3: Criar `app/manifest.ts`**

```ts
import type { MetadataRoute } from "next";

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: "Prumo · Sites, estratégia e presença digital",
    short_name: "Prumo",
    description:
      "Estúdio digital. Sites sob medida, planos de manutenção e parceria contínua.",
    start_url: "/",
    display: "standalone",
    background_color: "#000000",
    theme_color: "#000000",
    icons: [
      { src: "/icon.png", sizes: "any", type: "image/png" },
      { src: "/apple-icon.png", sizes: "any", type: "image/png" },
    ],
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/app/manifest.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
cd /e/projetos_breq/prumo && git add web/app/manifest.ts web/tests/app/manifest.test.ts && git commit -m "feat(seo): web app manifest + testes"
```

---

## Task 9: OG image própria de `/contato`

**Files:**
- Create: `app/contato/opengraph-image.tsx`

**Interfaces:**
- Consumes: `OG_SIZE`, `OG_CONTENT_TYPE`, `OgCard`, `loadInstrumentSerif` de `@/lib/og-template`.

- [ ] **Step 1: Criar `app/contato/opengraph-image.tsx`** (segue o padrão de `app/opengraph-image.tsx`)

```tsx
import { ImageResponse } from "next/og";
import {
  OG_SIZE,
  OG_CONTENT_TYPE,
  OgCard,
  loadInstrumentSerif,
} from "@/lib/og-template";

export const size = OG_SIZE;
export const contentType = OG_CONTENT_TYPE;
export const alt = "Contato · Prumo";

export default async function Image() {
  const font = await loadInstrumentSerif();
  return new ImageResponse(
    <OgCard eyebrow="Contato" title="Vamos conversar sobre o seu projeto." />,
    {
      ...OG_SIZE,
      fonts: [{ name: "Instrument Serif", data: font, style: "normal" }],
    },
  );
}
```

- [ ] **Step 2: Build (gera a rota da imagem)**

Run: `npm run build`
Expected: success; a rota `/contato/opengraph-image` aparece na lista de rotas.

- [ ] **Step 3: Commit**

```bash
cd /e/projetos_breq/prumo && git add web/app/contato/opengraph-image.tsx && git commit -m "feat(seo): OG image propria de /contato"
```

---

## Task 10: Config de domínio & Search Console

**Files:**
- Create/Modify: `.env.example`

**Interfaces:**
- Consumes: `NEXT_PUBLIC_SITE_URL` (já lido por `lib/site.ts`); `GOOGLE_SITE_VERIFICATION` (lido no `app/layout.tsx`, Task 2).

- [ ] **Step 1: Documentar as envs em `.env.example`**

Garanta que `.env.example` contenha (adicione se faltar, sem remover o que já existe):

```
# URL canônica do site (domínio de marca). Usada por metadataBase, canonicals, sitemap, robots, OG.
NEXT_PUBLIC_SITE_URL=https://SEU-DOMINIO.com.br

# Token de verificação do Google Search Console (meta tag pública)
GOOGLE_SITE_VERIFICATION=
```

- [ ] **Step 2: Commit**

```bash
cd /e/projetos_breq/prumo && git add web/.env.example && git commit -m "docs(seo): documenta NEXT_PUBLIC_SITE_URL e GOOGLE_SITE_VERIFICATION"
```

- [ ] **Step 3: Passos manuais (Guilherme — fora do código)**

  1. Na **Vercel → Project → Settings → Environment Variables (Production)**: setar `NEXT_PUBLIC_SITE_URL=https://<dominio>` e `GOOGLE_SITE_VERIFICATION=<token>`.
  2. Na **Vercel → Domains**: apontar o domínio; escolher canônico (recomendo **apex**) e 301 do `www` (ou vice-versa).
  3. Redeploy para as envs entrarem no build.
  4. **Google Search Console**: adicionar a propriedade, validar (a meta tag já sai no HTML via `verification.google`), e **submeter `https://<dominio>/sitemap.xml`**.

---

## Task 11: Validação final (barra de qualidade)

**Files:** nenhum (verificação).

- [ ] **Step 1: Lint**

Run: `npm run lint`
Expected: 0 erros (warnings pré-existentes de outros arquivos são aceitáveis).

- [ ] **Step 2: Testes**

Run: `npx vitest run`
Expected: todos verdes (inclui `tests/lib/schema.test.ts`, `tests/app/sitemap.test.ts`, `tests/app/manifest.test.ts` + suíte existente).

- [ ] **Step 3: Build**

Run: `npm run build`
Expected: success; rotas novas `/manifest.webmanifest` e `/contato/opengraph-image` na lista.

- [ ] **Step 4: Validação de rich results (manual, pós-deploy)**

Colar o HTML de `/`, `/planos` e um case no **Rich Results Test** (search.google.com/test/rich-results) ou **Schema Markup Validator** (validator.schema.org). Esperado: `Organization`/`WebSite`, `Service`×6, `CreativeWork` válidos, sem erros.

---

## Notas de execução
- Tasks 1, 7, 8 têm testes unitários reais (funções puras). Tasks de wiring (2-6, 9) usam `build` + checagem via `curl` no dev como gate, porque são Server Components async (difíceis de unit-testar com o setup atual).
- Nada aqui depende do domínio real **exceto** o valor final das envs (Task 10). Todo o código pode ser implementado e commitado antes de o domínio estar configurado — os schemas usam `SITE_URL`, que resolve sozinho quando a env entrar.
